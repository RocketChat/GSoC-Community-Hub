#!/bin/bash

# set -ex
git rev-parse --show-toplevel || echo .

# Get the current working directory
# current_dir=$(pwd)

# Use dirname twice to go up two levels
# two_levels_up=$(dirname "$(dirname "$current_dir")")

# echo "$two_levels_up"
